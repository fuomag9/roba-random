//
// Created by fuomag9 on 28/12/2019.
//

#include "elementUtil.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "generalUtil.h"
#include "elementUtil.h"
#include "esercizio_esame_librerie/strutture.h"

/*
void printElement(Evento el) {
    int i;
    printf("   %s %s, %s: ", el.id, el.surname, el.name);
    printf("%d esami superati.\n", el.num_votes);
    printf("     Voti: ");
    for (i = 0; i < el.num_votes; i++) {
        printf("%d", el.votes[i]);
        if (i < el.num_votes - 1)printf(" ");
    }
}

int compareElement(Evento el1, Evento el2) {
    int result;
    result = strcmp(el1.surname, el2.surname);
    if (result == 0) {
        result = strcmp(el1.name, el2.name);
        if (result == 0) {
            result = strcmp(el1.id, el2.id);
            if (result == 0) {
                if (el1.num_votes < el2.num_votes)
                    result = -1;
                else if(el1.num_votes > el2.num_votes)
                result = 1;
            }
        }
    }
    return result;
}

int equals(Evento el1, Evento el2) {
    if (strcmp(el1.id, el2.id) == 0 && strcmp(el1.surname, el2.surname) == 0 && strcmp(el1.name, el2.name) == 0)
        return 1;
    else
        return 0;
}
*/

